# OPENCL Rust project

## Synopsys 
	- Write an arbitrary `Rust` program that will leverage the integrated `Intel` graphics included in my CPU.
	- Practice using `Rust` to use the `GPGPU` standard.
	
